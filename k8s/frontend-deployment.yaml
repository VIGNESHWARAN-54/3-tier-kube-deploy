apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: incident-management
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: vigneshwaran54/incident-frontend:latest
          ports:
            - containerPort: 80
