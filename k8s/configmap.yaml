apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: incident-management
data:
  NODE_ENV: "production"
  PORT: "5000"
  MONGO_HOST: "mongodb-service"
  MONGO_PORT: "27017"
  DATABASE_NAME: "incident_management"
